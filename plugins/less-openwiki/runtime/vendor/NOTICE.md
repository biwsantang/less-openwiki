# Bundled dependency notice

`yaml.mjs` is a browser-targeted, minified bundle of `yaml` version 2.9.0.
It is included so the hook runtime can parse the same YAML front matter without
requiring packages to be installed in either supported plugin host.

`yaml` is distributed under the ISC License. Its source and license are
available at https://github.com/eemeli/yaml.
